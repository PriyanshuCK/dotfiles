# Mac only. Stowed by `stow local-mac`.
#
# claude, opencode and codex are native binaries already on PATH here, so
# they are deliberately not aliased — the WSL box aliases them to .exe paths
# only because it has no Linux build to run.

alias cl='claude'
alias oc='opencode'
alias cx='codex'
